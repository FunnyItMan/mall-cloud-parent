server:
  port: 8761  #端口号
spring:
  application:
    name: eureka-server   #服务名称
eureka:
  instance:
    prefer-ip-address: true
    instance-id: ${spring.application.name}:${server.port}
  client:
    fetch-registry: false
    register-with-eureka: false
    service-url:
      defaultZone: http://101.43.34.193:8761/eureka/